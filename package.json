{
  "name": "cowhand",
  "version": "1.0.0",
  "private": true,
  "description": "Orçamento familiar compartilhado (Henrique & Carol) — self-hosted",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "python build_app.py"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0"
  }
}
